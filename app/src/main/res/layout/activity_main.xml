<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.acer.mypda.MainActivity"
    android:weightSum="1"
    android:background="@drawable/background">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:srcCompat="@drawable/pda_title"
        android:id="@+id/PDA_title"
        android:layout_weight="0.2"/>

    <ImageButton
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:srcCompat="@drawable/to_do_button"
        android:id="@+id/todoButton"
        android:layout_weight="0.2" />

    <ImageButton
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:srcCompat="@drawable/time_log_button"
        android:id="@+id/timelogButton"
        android:layout_weight="0.2"/>

    <ImageButton
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:srcCompat="@drawable/expenses_button"
        android:id="@+id/expensesButton"
        android:layout_weight="0.2"/>

    <ImageButton
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:srcCompat="@drawable/exercise_log_button"
        android:id="@+id/exerciselogButton"
        android:layout_weight="0.2"/>


</LinearLayout>
